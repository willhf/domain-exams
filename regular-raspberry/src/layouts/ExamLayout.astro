---
import Layout from "./Layout.astro";
import "katex/dist/katex.min.css";

type Frontmatter = {
  title: string;
  description?: string;
};

const { title, description } = Astro.props.frontmatter as Frontmatter;
---

<Layout title={title} description={description}>
  <nav slot="breadcrumbs" class="breadcrumbs" aria-label="Breadcrumb">
    <a href="/">Domain Exams</a>
    <span class="sep">›</span>
    <span class="current">{title}</span>
  </nav>
  <slot />
</Layout>

<style>
  .breadcrumbs {
    font-size: 16px;
    color: #555;
    margin-bottom: 18px;
    max-width: var(--text-width);
  }

  .breadcrumbs a {
    color: inherit;
    text-decoration: none;
  }

  .breadcrumbs a:hover {
    text-decoration: underline;
  }

  .breadcrumbs .sep {
    margin: 0 6px;
    color: #aaa;
  }
</style>
